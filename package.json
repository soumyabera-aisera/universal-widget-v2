{
  "name": "universal-widget-v2",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "build:zendesk": "tsc && vite build --config vite.config.zendesk.ts",
    "build:servicenow": "vite build --config vite.config.servicenow.ts"
  },
  "dependencies": {
    "lit": "^3.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.9",
    "typescript": "^5.2.2",
    "vite": "^5.2.0"
  }
}
